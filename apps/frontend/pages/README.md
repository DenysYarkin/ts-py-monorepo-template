## Why does this directory exist?

This project follows FSD principles, which, however, partially conflict with basic NextJS project structure.
This is a mock directory since we are using app router which also supports `pages/` directory, this folder is needed,
but should remain empty.

More details:
https://feature-sliced.github.io/documentation/docs/guides/tech/with-nextjs
