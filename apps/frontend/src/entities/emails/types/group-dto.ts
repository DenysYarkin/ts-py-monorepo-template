export type EmailsGroupDto = {
  group_id: string;
  name: string;
}

export type CreateGroupDto = {
  group_id: string,
  name: string;
}

export type CreateGroupResponseDto = {
  group_id: string,
  name: string;
}
